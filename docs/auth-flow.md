# Authentication Flow# Authentication Flow (JWT)# Authentication Flow (JWT)# 🔐 Authentication Flow (JWT)



```mermaid

sequenceDiagram

  participant User```mermaid

  participant Frontend

  participant BackendsequenceDiagram

  participant DB

  participant User```mermaid```mermaid

  User->>Frontend: Enters username & password

  Frontend->>Backend: POST /api/auth/login  participant Frontend

  Backend->>DB: Validate credentials

  DB-->>Backend: Valid  participant BackendsequenceDiagramsequenceDiagram

  Backend-->>Frontend: JWT Token

  Frontend->>User: Stores token in localStorage  participant DB

  User->>Frontend: Accesses /notes page

  Frontend->>Backend: GET /api/notes (Authorization: Bearer token)  participant User  participant User

  Backend->>Backend: Validate JWT & load user context

  Backend-->>Frontend: Return notes JSON  User->>Frontend: Enters username & password

```

  Frontend->>Backend: POST /api/auth/login  participant Frontend  participant Frontend

## Description

  Backend->>DB: Validate credentials

- Authentication uses JWT tokens generated by the backend.

- Each request to protected endpoints includes the token in the Authorization header.  DB-->>Backend: Valid  participant Backend  participant Backend

- Tokens are verified on every call, and user context is extracted from them.

  Backend-->>Frontend: JWT Token

  Frontend->>User: Stores token in localStorage  participant DB  participant DB

  User->>Frontend: Accesses /notes page

  Frontend->>Backend: GET /api/notes (Authorization: Bearer <token>)

  Backend->>Backend: Validate JWT & load user context

  Backend-->>Frontend: Return notes JSON  User->>Frontend: Enters username & password  User->>Frontend: Enters username & password

```

  Frontend->>Backend: POST /api/auth/login  Frontend->>Backend: POST /api/auth/login

## Description

  Backend->>DB: Validate credentials  Backend->>DB: Validate credentials

- Authentication uses JWT tokens generated by the backend.

- Each request to protected endpoints includes the token in the Authorization header.  DB-->>Backend: Valid  DB-->>Backend: Valid

- Tokens are verified on every call, and user context is extracted from them.

  Backend-->>Frontend: JWT Token  Backend-->>Frontend: JWT Token

  Frontend->>User: Stores token in localStorage  Frontend->>User: Stores token in localStorage

  User->>Frontend: Accesses /notes page  User->>Frontend: Accesses /notes page

  Frontend->>Backend: GET /api/notes (Authorization: Bearer <token>)  Frontend->>Backend: GET /api/notes (Authorization: Bearer <token>)

  Backend->>Backend: Validate JWT & load user context  Backend->>Backend: Validate JWT & load user context

  Backend-->>Frontend: Return notes JSON  Backend-->>Frontend: Return notes JSON

``````



## Description## Description



- Authentication uses JWT tokens generated by the backend.- Authentication uses JWT tokens generated by the backend.

- Each request to protected endpoints includes the token in the Authorization header.- Each request to protected endpoints includes the token in the Authorization header.

- Tokens are verified on every call, and user context is extracted from them.- Tokens are verified on every call, and user context is extracted from them.

